/*import { Swiper } from "swiper/vue";



function projectsSwiper() {
  const mySwiper = new Swiper('.swiper-container', {
    // settings Swiper

    direction: 'horizontal',
    slidesPerView: 1,
    slidesPerGroup: 1,
    spaceBetween: 0,
    loop: false,
    autoHeight: false,

  });

  const btnSwiperLeft = document.querySelectorAll('.swiper-button-prev');
  const btnSwiperRight = document.querySelectorAll('.swiper-button-next');



  btnSwiperRight.forEach(btn =>
    btn.addEventListener('click', () => {
      document.querySelectorAll('.swiper-slide').forEach(item => {
        if (item.classList.contains('is-active')) {
          Swiperper.btnSwiperRight
        } else {
          item.firstElementChild.style.marginBottom = '35px';
          item.firstElementChild.style.transition = 'margin 0.5s ease-in-out';
        }
      });
    })
  );
}
ProjectsSection();*/